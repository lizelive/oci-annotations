on:
  push:
    branches:
      - main
name: release pipeline
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: rust
          package-name: oci-annotations